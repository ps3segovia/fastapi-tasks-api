# Tasks API

Простой pet-проект на **FastAPI** для управления задачами.

## 🚀 Возможности

- `GET /` — приветственное сообщение
- `GET /hello/{name}` — приветствие с валидацией имени (2–20 символов)
- `GET /tasks` — получить список задач
- `POST /tasks` — создать новую задачу

## ▶️ Как запустить

1. Установи зависимости:
   ```bash
   pip install -r requirements.txt
